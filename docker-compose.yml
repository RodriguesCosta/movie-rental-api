version: "3"

services:
  mongodb:
    image: docker.io/mongo:7
    restart: always

  app:
    image: app-local
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    ports:
      - 3333:3333
    depends_on:
      - mongodb
    env_file:
      - .env.example
